apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: k8s-state-core-workload
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
    - matrix:
        generators:
          - merge:
              mergeKeys:
                - appName
              generators:
                - git:
                    repoURL: https://github.com/hugolhafner/k8s-state-core-workload.git
                    revision: HEAD
                    files:
                      - path: apps/*/config.json
                    pathParamPrefix: baseApp
                    values:
                      appName: {{ printf "%s" "{{ .baseApp.path.basenameNormalized }}" | quote }}

                - git:
                    repoURL: https://github.com/hugolhafner/k8s-state-core-workload.git
                    revision: HEAD
                    files:
                      - path: "clusters/{{ .Values.clusterName }}/apps/{{ printf "%s" "{{ .values.appName }}" }}/config.json"
                    pathParamPrefix: clusterApp

          - list:
              elements: {{ .Values.environments | toYaml | nindent 16 }}

  template:
    metadata:
      name: {{ printf "%s-%s" "{{ .appName }}" "{{ .environment }}" | quote }}
    spec:
      project: k8s-state-core-workload
      sources:
        - repoURL: {{ printf "%s" "{{ .source.repoURL }}" | quote }}
          targetRevision: {{ printf "%s" "{{ .appName }}" | quote }}
          chart: {{ printf "%s" "{{ .appName }}" | quote }}
          helm:
            releaseName: {{ printf "%s-%s" "{{ .appName }}" "{{ .environment }}" | quote }}
            ignoreMissingValueFiles: true
            valueFiles:
              - {{ printf "$values/apps/%s/values.yaml" "{{ .appName }}" | quote }}
              - {{ printf "$values/apps/%s/environments/%s.yaml" "{{ .appName }}" "{{ .environment }}" | quote }}
              - "$values/clusters/{{ .Values.clusterName }}/apps/{{ printf "%s" "{{ .appName }}" }}/values.yaml"
              - "$values/clusters/{{ .Values.clusterName }}/apps/{{ printf "%s/environments/%s.yaml" "{{ .appName }}" "{{ .environment }}" }}"
        - repoURL: https://github.com/hugolhafner/k8s-state-core-workload.git
          targetRevision: HEAD
          ref: values
      destination:
        server: https://kubernetes.default.svc
        namespace: {{ printf "%s" "{{ .environment }}" | quote }}
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true

      ignoreDifferences:
        - group: argoproj.io
          kind: Application
          jsonPointers:
            /spec/syncPolicy/automated
